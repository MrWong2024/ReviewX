# ReviewX 后端配置矩阵

## 1. 用途

- 记录后端环境变量、配置项、默认值、运行环境差异、敏感性和来源
- 防止 Codex 或人工维护者误用配置

## 2. 当前状态

- `backend` 已初始化为公共 NestJS 骨架
- 当前已接入 `MongooseModule` 与 MongoDB 连接配置基线
- 配置值当前来自环境变量，并提供 `.env.development.example` / `.env.test.example` / `.env.production.example`
- 当前已注册 users、sessions 和第一阶段业务 Schema
- 当前已接入合议草稿 LLM 调用服务，使用通用 `LLM_PROVIDER` 与 Bailian OpenAI-compatible Chat 配置；未配置或调用失败时业务层 fallback 到 `rule_based`
- 当前 MongoDB 配置已区分应用运行连接 `MONGO_URI` 与运维脚本连接 `MONGO_ADMIN_URI`
- 当前 `scripts/sync-indexes.ts` 已使用 `MONGO_ADMIN_URI` 执行受控索引同步
- 当前 `scripts/sync-indexes.ts` 覆盖 `User`、`Session`、`Batch`、`Dictionary`、`TreeDictionary`、`Organization`、`ReviewScheme`、`Project`、`ProjectExpertAssignment`、`ProjectMaterial`、`ProjectMaterialDeletionLog`、`ProjectImportFieldMapping`、`ProjectImportJob`、`ProjectImportRow`、`ExpertReview`、`ConsensusReview`、`ProjectAppeal`、`ProjectAppealAttachment`、`ProjectLevelChangeLog`
- 当前已新增 session / Cookie 配置基线，用于 auth 第一阶段登录态
- 当前 `dca16ae` 基线已安装 `ali-oss`
- 当前已实现 Storage 抽象层并消费 Storage / OSS 环境变量；第五阶段专家评分与合议评审未新增环境变量；第六阶段项目申诉与等级变更留痕未新增环境变量；管理员用户维护 API 也未新增环境变量；Excel 字段映射配置后端化未新增环境变量；第四阶段补丁一门户端只读基础数据接口未新增环境变量；第四阶段补丁三项目材料草稿/提交与物理删除后端化未新增环境变量、第三方依赖或 OSS 配置
- 当前已新增短信验证码登录和短信验证码找回密码后端配置，复用 `SMS_AUTH_PROVIDER` 和 `ALIYUN_SMS_*`；test 环境强制 stub，不调用真实阿里云短信 API；production 默认建议 aliyun

## 3. 当前配置项

| 配置项                              | 所属模块    | 类型    | 是否敏感 | development                                                                                                                        | test                                                                                                                                   | staging  | production                                                                                                                                           | 默认值                                                           | 来源     | 是否必填                                     | 说明                                                                                                       |
| ----------------------------------- | ----------- | ------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------- | -------- | -------------------------------------------- | ---------------------------------------------------------------------------------------------------------- |
| `NODE_ENV`                          | app         | string  | 否       | `development`                                                                                                                      | `test`                                                                                                                                 | 环境变量 | 环境变量                                                                                                                                             | `development`                                                    | 环境变量 | 否                                           | 运行环境标识，校验范围为 `development / test / production`                                                 |
| `PORT`                              | app         | number  | 否       | `5001` 或环境变量覆盖                                                                                                              | 环境变量或默认值                                                                                                                       | 环境变量 | 环境变量                                                                                                                                             | `5001`                                                           | 环境变量 | 否                                           | HTTP 监听端口；ReviewX 统一使用标准 `PORT` 变量名                                                          |
| `FRONTEND_URL`                      | app         | string  | 否       | `http://localhost:3001` 或环境变量覆盖                                                                                             | 环境变量或默认值                                                                                                                       | 环境变量 | 环境变量                                                                                                                                             | `http://localhost:3001`                                          | 环境变量 | 否                                           | 前端地址，作为 CORS 备用来源                                                                               |
| `CORS_ORIGIN`                       | app         | string  | 否       | `http://localhost:3001` 或环境变量覆盖                                                                                             | 环境变量或默认值                                                                                                                       | 环境变量 | 环境变量                                                                                                                                             | `http://localhost:3001`                                          | 环境变量 | 否                                           | CORS 来源配置；与 `FRONTEND_URL` 语义不同，支持单值、逗号分隔多值或 `*`                                    |
| `MONGO_URI`                         | mongo       | string  | 是       | `mongodb://reviewx_dev_app:{REVIEWX_DEV_APP_PASSWORD}@127.0.0.1:27017/reviewx_dev?authSource=reviewx_dev` 或环境变量覆盖           | `mongodb://reviewx_test_app:{REVIEWX_TEST_APP_PASSWORD}@127.0.0.1:27017/reviewx_test?authSource=reviewx_test` 或环境变量覆盖           | 环境变量 | `mongodb://reviewx_app:{URL_ENCODED_PASSWORD}@{HOST1}:3717,{HOST2}:3717/reviewx?authSource=admin&replicaSet={REPLICA_SET}` 或环境变量覆盖            | development/test 使用本地应用账号默认值；production 无代码默认值 | 环境变量 | production 是；development/test 默认值已提供 | 应用运行 MongoDB 连接串；`AppModule` / `MongooseModule` 只使用该项；受控索引同步不得使用该项             |
| `MONGO_ADMIN_URI`                   | mongo       | string  | 是       | `mongodb://reviewx_dev_db_admin:{REVIEWX_DEV_DB_ADMIN_PASSWORD}@127.0.0.1:27017/reviewx_dev?authSource=reviewx_dev` 或环境变量覆盖 | `mongodb://reviewx_test_db_admin:{REVIEWX_TEST_DB_ADMIN_PASSWORD}@127.0.0.1:27017/reviewx_test?authSource=reviewx_test` 或环境变量覆盖 | 环境变量 | `mongodb://reviewx_db_admin:{URL_ENCODED_ADMIN_PASSWORD}@{HOST1}:3717,{HOST2}:3717/reviewx?authSource=admin&replicaSet={REPLICA_SET}` 或环境变量覆盖 | development/test 使用本地运维账号默认值；production 无代码默认值 | 环境变量 | production 是；development/test 默认值已提供 | 索引同步、迁移和运维脚本连接串；`scripts/sync-indexes.ts` 使用该项；当前应用运行不读取该项               |
| `MONGO_AUTO_INDEX`                  | mongo       | boolean | 否       | `true` 或环境变量覆盖                                                                                                              | `true` 或环境变量覆盖                                                                                                                  | 环境变量 | `false`                                                                                                                                              | development/test 默认 `true`；production 默认 `false`            | 环境变量 | 否                                           | 统一控制 Mongoose `autoIndex`；production 必须关闭，生产索引通过 `scripts/sync-indexes.ts` 受控同步       |
| `MONGO_SERVER_SELECTION_TIMEOUT_MS` | mongo       | number  | 否       | `5000` 或环境变量覆盖                                                                                                              | `5000` 或环境变量覆盖                                                                                                                  | 环境变量 | `5000` 或环境变量覆盖                                                                                                                                | `5000`                                                           | 环境变量 | 否                                           | MongoDB server selection timeout，当前已接入 `MongooseModule` 连接参数以便尽快 fail fast                   |
| `STORAGE_DRIVER`                    | storage     | string  | 否       | `fake`                                                                                                                            | `fake`                                                                                                                                 | 环境变量 | `oss`                                                                                                                                                | production 默认为 `oss`，其他环境默认为 `fake`                  | 环境变量 | 否                                           | 存储驱动，支持 `fake / oss`；`fake` 不访问真实 OSS，`oss` 使用 `ali-oss`                                  |
| `OSS_REGION`                        | storage.oss | string  | 否       | `oss-cn-shenzhen`                                                                                                                 | `oss-cn-shenzhen`                                                                                                                      | 环境变量 | `oss-cn-shenzhen`                                                                                                                                   | 无                                                               | 环境变量 | `STORAGE_DRIVER=oss` 且调用真实能力时是       | 阿里云 OSS 地域；`STORAGE_DRIVER=fake` 时不要求配置完整                                                    |
| `OSS_BUCKET`                        | storage.oss | string  | 否       | `reviewx-prod-materials`                                                                                                          | `reviewx-test-materials`                                                                                                               | 环境变量 | `reviewx-prod-materials`                                                                                                                            | fake storage 使用 `fake-reviewx-materials`                      | 环境变量 | `STORAGE_DRIVER=oss` 且调用真实能力时是       | OSS Bucket 名称；Bucket 建议私有读写，后端生成短期签名 URL 供下载                                        |
| `OSS_INTERNAL_ENDPOINT`             | storage.oss | string  | 否       | `oss-cn-shenzhen-internal.aliyuncs.com`                                                                                           | `oss-cn-shenzhen-internal.aliyuncs.com`                                                                                                | 环境变量 | `oss-cn-shenzhen-internal.aliyuncs.com`                                                                                                             | 无                                                               | 环境变量 | `STORAGE_DRIVER=oss` 且调用真实能力时是       | 后端部署在阿里云同地域 ECS 时上传/删除 OSS 使用的内网 endpoint；本地开发机器通常不能访问                  |
| `OSS_PUBLIC_ENDPOINT`               | storage.oss | string  | 否       | `oss-cn-shenzhen.aliyuncs.com`                                                                                                    | `oss-cn-shenzhen.aliyuncs.com`                                                                                                         | 环境变量 | `oss-cn-shenzhen.aliyuncs.com`                                                                                                                      | 无                                                               | 环境变量 | `STORAGE_DRIVER=oss` 且调用签名 URL 时是      | 生成浏览器可访问的签名下载 URL 使用的公网 endpoint                                                        |
| `OSS_ACCESS_KEY_ID`                 | storage.oss | string  | 是       | `your-ram-access-key-id`                                                                                                          | `test-placeholder`                                                                                                                     | 环境变量 | `your-ram-access-key-id`                                                                                                                            | 无                                                               | 环境变量 | `STORAGE_DRIVER=oss` 且调用真实能力时是       | RAM 用户 AccessKeyId；example 只能使用占位符，不得提交真实值，不得使用阿里云主账号 AccessKey              |
| `OSS_ACCESS_KEY_SECRET`             | storage.oss | string  | 是       | `your-ram-access-key-secret`                                                                                                      | `test-placeholder`                                                                                                                     | 环境变量 | `your-ram-access-key-secret`                                                                                                                        | 无                                                               | 环境变量 | `STORAGE_DRIVER=oss` 且调用真实能力时是       | RAM 用户 AccessKeySecret；example 只能使用占位符，不得提交真实值，后续可替换为 RAM Role                  |
| `OSS_OBJECT_PREFIX`                 | storage.oss | string  | 否       | `reviewx`                                                                                                                         | `reviewx-test`                                                                                                                         | 环境变量 | `reviewx`                                                                                                                                           | `reviewx`                                                        | 环境变量 | 否                                           | OSS object key 前缀；项目材料 objectKey 形如 `{prefix}/projects/{projectId}/materials/{materialTypeCode}/{yyyy}/{uuid}-{safeFilename}`；申诉附件 objectKey 形如 `{prefix}/projects/{projectId}/appeals/{appealId}/{yyyy}/{uuid}-{safeFilename}` |
| `SESSION_COOKIE_NAME`               | session     | string  | 否       | `reviewx_session` 或环境变量覆盖                                                                                                   | `reviewx_session` 或环境变量覆盖                                                                                                       | 环境变量 | `reviewx_session` 或环境变量覆盖                                                                                                                     | `reviewx_session`                                                | 环境变量 | 否                                           | HttpOnly Cookie 名称；Cookie 内容只保存 session token                                                     |
| `SESSION_TTL_MS`                    | session     | number  | 否       | `86400000` 或环境变量覆盖                                                                                                          | `86400000` 或环境变量覆盖                                                                                                              | 环境变量 | `86400000` 或环境变量覆盖                                                                                                                            | `86400000`                                                       | 环境变量 | 否                                           | session TTL，Cookie Max-Age 与该值对齐                                                                     |
| `MAX_ACTIVE_SESSIONS_PER_USER`      | session     | number  | 否       | `5` 或环境变量覆盖                                                                                                                 | `5` 或环境变量覆盖                                                                                                                     | 环境变量 | `5` 或环境变量覆盖                                                                                                                                   | `5`                                                              | 环境变量 | 否                                           | 登录成功后按该上限回收旧 session                                                                           |
| `SESSION_COOKIE_SECURE`             | session     | boolean | 否       | `false` 或环境变量覆盖                                                                                                             | `false` 或环境变量覆盖                                                                                                                 | 环境变量 | `true` 默认；正式 HTTPS 部署建议保持 `true`；无 HTTPS 的 IP / 内网 / 临时部署可显式 `false`                                                          | production 默认 `true`；其他环境默认 `false`                    | 环境变量 | 否                                           | 控制 Cookie `Secure` 属性；`SameSite=none` 时必须为 `true`；关闭 Secure 不再需要修改服务器源码          |
| `SESSION_COOKIE_SAME_SITE`          | session     | string  | 否       | `lax` 或环境变量覆盖                                                                                                               | `lax` 或环境变量覆盖                                                                                                                   | 环境变量 | `lax` 或环境变量覆盖                                                                                                                                 | `lax`                                                            | 环境变量 | 否                                           | 允许 `lax / strict / none`；如设置 `none`，`SESSION_COOKIE_SECURE` 必须为 `true`                         |
| `SMS_AUTH_PROVIDER`                 | auth.sms    | string  | 否       | `stub` 或环境变量覆盖                                                                                                              | `stub`；自动化测试强制 stub                                                                                                            | 环境变量 | `aliyun` 或环境变量覆盖                                                                                                                              | production 默认 `aliyun`；其他环境默认 `stub`                   | 环境变量 | 否                                           | 短信认证 provider，支持 `stub / aliyun`；stub 仅用于本地开发和自动化测试，不应用于正式环境              |
| `ALIYUN_SMS_ACCESS_KEY_ID`          | auth.sms    | string  | 是       | 空字符串或环境变量覆盖                                                                                                             | 空字符串                                                                                                                               | 环境变量 | 环境变量                                                                                                                                             | 空字符串                                                         | 环境变量 | `SMS_AUTH_PROVIDER=aliyun` 且调用真实能力时是 | 阿里云号码认证 AccessKeyId；不得写入仓库；缺失时真实发送/校验返回服务未配置类错误                       |
| `ALIYUN_SMS_ACCESS_KEY_SECRET`      | auth.sms    | string  | 是       | 空字符串或环境变量覆盖                                                                                                             | 空字符串                                                                                                                               | 环境变量 | 环境变量                                                                                                                                             | 空字符串                                                         | 环境变量 | `SMS_AUTH_PROVIDER=aliyun` 且调用真实能力时是 | 阿里云号码认证 AccessKeySecret；不得写入仓库；不得记录到日志或响应                                       |
| `ALIYUN_SMS_REGION_ID`              | auth.sms    | string  | 否       | `cn-shenzhen` 或环境变量覆盖                                                                                                       | `cn-shenzhen`                                                                                                                          | 环境变量 | `cn-shenzhen` 或环境变量覆盖                                                                                                                         | `cn-shenzhen`                                                    | 环境变量 | 否                                           | 阿里云号码认证服务地域，当前口径为华南1（深圳）                                                          |
| `ALIYUN_SMS_ENDPOINT`               | auth.sms    | string  | 否       | `dypnsapi.aliyuncs.com` 或环境变量覆盖                                                                                             | `dypnsapi.aliyuncs.com`                                                                                                                | 环境变量 | `dypnsapi.aliyuncs.com` 或环境变量覆盖                                                                                                               | `dypnsapi.aliyuncs.com`                                          | 环境变量 | 否                                           | 阿里云号码认证 OpenAPI endpoint；请求 URL 含签名，禁止写日志                                            |
| `ALIYUN_SMS_COUNTRY_CODE`           | auth.sms    | string  | 否       | `86` 或环境变量覆盖                                                                                                                | `86`                                                                                                                                   | 环境变量 | `86` 或环境变量覆盖                                                                                                                                  | `86`                                                             | 环境变量 | 否                                           | 当前仅支持国内手机号 `CountryCode=86`                                                                     |
| `ALIYUN_SMS_SIGN_NAME`              | auth.sms    | string  | 否       | `速通互联验证码` 或环境变量覆盖                                                                                                    | `速通互联验证码`                                                                                                                       | 环境变量 | `速通互联验证码` 或环境变量覆盖                                                                                                                      | `速通互联验证码`                                                 | 环境变量 | 真实发送时是                                | 阿里云短信签名名称                                                                                        |
| `ALIYUN_SMS_TEMPLATE_CODE`          | auth.sms    | string  | 否       | `100001` 或环境变量覆盖                                                                                                            | `100001`                                                                                                                               | 环境变量 | `100001` 或环境变量覆盖                                                                                                                              | `100001`                                                         | 环境变量 | 真实发送时是                                | 阿里云号码认证短信模板 Code                                                                               |
| `ALIYUN_SMS_TEMPLATE_PARAM`         | auth.sms    | string  | 否       | `{"code":"##code##","min":"5"}` 或环境变量覆盖                                                                                     | `{"code":"##code##","min":"5"}`                                                                                                        | 环境变量 | `{"code":"##code##","min":"5"}` 或环境变量覆盖                                                                                                      | `{"code":"##code##","min":"5"}`                                  | 环境变量 | 真实发送时是                                | 必须保留 `##code##`，验证码由阿里云生成和校验；后端不保存明文验证码                                      |
| `ALIYUN_SMS_CODE_LENGTH`            | auth.sms    | number  | 否       | `6` 或环境变量覆盖                                                                                                                 | `6`                                                                                                                                    | 环境变量 | `6` 或环境变量覆盖                                                                                                                                   | `6`                                                              | 环境变量 | 否                                           | 验证码长度，校验范围 4..8；当前 DTO 按 6 位数字校验                                                      |
| `ALIYUN_SMS_VALID_TIME_SECONDS`     | auth.sms    | number  | 否       | `300` 或环境变量覆盖                                                                                                               | `300`                                                                                                                                  | 环境变量 | `300` 或环境变量覆盖                                                                                                                                 | `300`                                                            | 环境变量 | 否                                           | 验证码有效期秒数，公开发送响应 `expiresInSeconds` 使用该值                                               |
| `ALIYUN_SMS_DUPLICATE_POLICY`       | auth.sms    | number  | 否       | `1` 或环境变量覆盖                                                                                                                 | `1`                                                                                                                                    | 环境变量 | `1` 或环境变量覆盖                                                                                                                                   | `1`                                                              | 环境变量 | 否                                           | 阿里云发送重复策略，允许 `1 / 2`                                                                          |
| `ALIYUN_SMS_INTERVAL_SECONDS`       | auth.sms    | number  | 否       | `60` 或环境变量覆盖                                                                                                                | `60`                                                                                                                                   | 环境变量 | `60` 或环境变量覆盖                                                                                                                                  | `60`                                                             | 环境变量 | 否                                           | 发送间隔秒数，公开发送响应 `cooldownSeconds` 使用该值                                                    |
| `ALIYUN_SMS_CODE_TYPE`              | auth.sms    | number  | 否       | `1` 或环境变量覆盖                                                                                                                 | `1`                                                                                                                                    | 环境变量 | `1` 或环境变量覆盖                                                                                                                                   | `1`                                                              | 环境变量 | 否                                           | 阿里云验证码类型，校验范围 1..7                                                                           |
| `ALIYUN_SMS_CASE_AUTH_POLICY`       | auth.sms    | number  | 否       | `1` 或环境变量覆盖                                                                                                                 | `1`                                                                                                                                    | 环境变量 | `1` 或环境变量覆盖                                                                                                                                   | `1`                                                              | 环境变量 | 否                                           | 阿里云验证码校验策略，允许 `1 / 2`                                                                        |
| `LLM_PROVIDER`                      | llm         | string  | 否       | `bailian` 或环境变量覆盖                                                                                                           | `stub` 或环境变量覆盖                                                                                                                  | 环境变量 | `bailian`                                                                                                                                            | `stub`                                                           | 环境变量 | 否                                           | 合议草稿 LLM 提供方标识；当前允许 `stub`、`bailian`；非 `bailian` 时 fallback 到 `rule_based`              |
| `BAILIAN_API_KEY`                   | llm.bailian | string  | 是       | `{BAILIAN_API_KEY}` 或环境变量覆盖                                                                                                 | 空字符串                                                                                                                               | 环境变量 | `{BAILIAN_API_KEY}` 或真实部署值                                                                                                                     | 空字符串                                                         | 环境变量 | 业务调用真实 AI 时是                         | 百炼 API Key；不得写入仓库；缺失时合议草稿 fallback，不在启动期崩溃                                      |
| `BAILIAN_BASE_URL`                  | llm.bailian | string  | 否       | `https://dashscope.aliyuncs.com/compatible-mode/v1`                                                                                | `https://dashscope.aliyuncs.com/compatible-mode/v1`                                                                                    | 环境变量 | `https://dashscope.aliyuncs.com/compatible-mode/v1`                                                                                                  | `https://dashscope.aliyuncs.com/compatible-mode/v1`              | 环境变量 | 业务调用真实 AI 时是                         | 百炼 OpenAI 兼容接口基地址；合议草稿服务调用 `{baseUrl}/chat/completions`                               |
| `BAILIAN_MODEL`                     | llm.bailian | string  | 否       | `qwen3.6-plus`                                                                                                                     | 空字符串                                                                                                                               | 环境变量 | `qwen3.6-plus`                                                                                                                                       | 空字符串                                                         | 环境变量 | 业务调用真实 AI 时是                         | 百炼模型名必须由 env 提供，代码中不固化默认模型；缺失时 fallback                                        |
| `BAILIAN_TIMEOUT_MS`                | llm.bailian | number  | 否       | `90000`                                                                                                                            | `90000`                                                                                                                                | 环境变量 | `90000`                                                                                                                                              | `90000`                                                          | 环境变量 | 否                                           | 百炼调用超时基线；合议草稿服务按请求期读取                                                                |
| `BAILIAN_MAX_RETRIES`               | llm.bailian | number  | 否       | `1`                                                                                                                                | `1`                                                                                                                                    | 环境变量 | `1`                                                                                                                                                  | `1`                                                              | 环境变量 | 否                                           | 百炼调用重试次数基线；合议草稿服务按请求期读取                                                            |
| `CONSENSUS_DRAFT_COOLDOWN_SECONDS` | consensus   | number  | 否       | `60` 或环境变量覆盖                                                                                                                | `60` 或环境变量覆盖                                                                                                                    | 环境变量 | `60` 或环境变量覆盖                                                                                                                                  | `60`                                                             | 环境变量 | 否                                           | 同项目生成合议草稿冷却秒数；非法、NaN 或小于 0 回退 60；只限制生成草稿，不限制查看、填入或确认           |

## 4. 配置分类建议

- 服务端口
- 数据库连接
- Session / Cookie
- 对象存储
- 邮件 / 短信
- 外部会议或直播平台
- 模型服务
- 日志与审计
- 后台任务
- 调试 / Ops 开关

## 5. 维护规则

- 新增或修改环境变量必须同步本文档
- 新增敏感配置必须标注敏感
- 不得在本文档写入真实密钥、密码、连接串或 Token
- 配置实际值以环境变量、部署平台或 `.env.development.example` / `.env.test.example` / `.env.production.example` 为准
- 真实 `.env` 文件必须保持在 `backend/.gitignore` 中，不进入 Git；当前 `backend/.gitignore` 忽略 `.env` 和 `.env.*`，并显式允许三类 example 文件
- production 数据库名口径为 `reviewx`，development 为 `reviewx_dev`，test 为 `reviewx_test`
- development/test 已按当前本地账号设计区分应用账号与运维账号
- 生产环境不依赖 `autoIndex` 自动建索引；需要显式执行 `npm run sync-indexes -- --env-file .env.production --confirm-production`
- 当前无 Email 或 bcrypt 配置；SMS 已新增 `SMS_AUTH_PROVIDER` 与 `ALIYUN_SMS_*`，用于短信验证码登录和短信验证码找回密码后端，不用于短信注册
- 当前有 session / Cookie 配置，但不包含用户信息、角色或业务权限配置；production 下 `SESSION_COOKIE_SECURE` 默认 `true`，正式 HTTPS 部署建议保持 `true`，无 HTTPS 的 IP / 内网 / 临时试跑可显式配置 `false`；`SESSION_COOKIE_SAME_SITE=none` 时仍必须 `SESSION_COOKIE_SECURE=true`，不再需要在服务器修改 `src/config/env.validation.ts`
- 当前已接入合议草稿 LLM / Bailian 调用服务；`LLM_PROVIDER` 非 `bailian`、`BAILIAN_API_KEY/BASE_URL/MODEL` 缺失、超时、HTTP 非 2xx 或响应不可解析时 fallback 到 `rule_based`
- Storage / OSS 使用既有环境变量：`STORAGE_DRIVER`、`OSS_REGION`、`OSS_BUCKET`、`OSS_INTERNAL_ENDPOINT`、`OSS_PUBLIC_ENDPOINT`、`OSS_ACCESS_KEY_ID`、`OSS_ACCESS_KEY_SECRET`、`OSS_OBJECT_PREFIX`；第四阶段未新增环境变量或第三方依赖
- E2E 测试不得依赖真实阿里云 OSS；test 环境默认 `STORAGE_DRIVER=fake`，当前自动化测试使用 `FakeStorageService`
- 生产对象存储口径为 `STORAGE_DRIVER=oss`，Bucket 建议私有读写，下载由后端生成短期签名 URL；默认有效期 10 分钟；文件预览 / 在线转换仍未实现
- `STORAGE_DRIVER=oss` 时应用启动不主动因 OSS 配置缺失崩溃；调用上传、删除或签名 URL 能力时校验缺失配置并返回明确服务不可用错误，不泄露 AccessKey
- 第二阶段新增 `project_import_jobs`、`project_import_rows` 关键索引已纳入 `scripts/sync-indexes.ts`；第二阶段补丁一新增 `project_import_field_mappings` 集合，索引包括 `standardField` unique、`isActive`、`normalizedAliases`，并已纳入 `scripts/sync-indexes.ts`；第三阶段新增 `project_expert_assignments` 关键索引已纳入；第四阶段新增 `project_materials` 关键索引已纳入；第四阶段补丁三新增 `project_material_deletion_logs` 关键索引已纳入；第五阶段新增 `expert_reviews`、`consensus_reviews` 关键索引已纳入；第六阶段新增 `project_appeals`、`project_appeal_attachments`、`project_level_change_logs` 关键索引已纳入；管理员用户维护 API 本阶段未新增 users 查询索引；production 仍必须使用 `--confirm-production`
- 第三阶段未新增环境变量、第三方依赖或腾讯会议相关配置；`meetingUrl` 仅作为 Project 字符串字段保存
- 合议草稿 AI 小修新增 `CONSENSUS_DRAFT_COOLDOWN_SECONDS`，默认 60；复用既有 `LLM_PROVIDER/BAILIAN_*`，不新增第三方依赖，不安装 OpenAI SDK，测试环境不得调用真实百炼 API
- 短信验证码登录和短信验证码找回密码后端复用 `SMS_AUTH_PROVIDER/ALIYUN_SMS_*`，不新增第三方依赖，不安装阿里云 SDK；使用 Node 内置 `fetch` 和 `crypto` 实现 RPC 签名；自动化测试使用 stub 和 mock fetch，不调用真实阿里云短信 API；验证码由阿里云生成和校验，后端不保存明文验证码
- 第六阶段未新增环境变量、第三方依赖或 OSS 配置；申诉附件继续复用既有 `STORAGE_DRIVER` 与 `OSS_*`，E2E 使用 fake storage，不依赖真实 OSS
- 管理员用户维护 API 未新增环境变量、第三方依赖、OSS 配置或 `sync-indexes.ts` 模型/索引注册；继续沿用 `users.phone` unique 索引和受控索引同步流程
- Excel 字段映射配置后端化未新增环境变量、第三方依赖、OSS 配置或正式产品配置项；production 索引不得依赖 `autoIndex`，新增 `project_import_field_mappings` 索引需按既有 `sync-indexes` 受控流程同步
- 第四阶段补丁一门户端只读基础数据接口未新增环境变量、第三方依赖、OSS 配置、数据库集合或 `sync-indexes.ts` 模型/索引注册；仅复用既有主数据和用户集合做只读摘要查询
