# ReviewX 后端 Service 职责地图

## 1. 用途

- 记录 Service 职责边界、依赖关系、外部副作用和不应承担的职责
- 防止 Service 职责膨胀或跨模块串台

## 2. 当前状态

- `backend` 已初始化
- 当前已确认最小根服务
- 当前已新增 users 基础 Service
- 当前已新增 sessions 基础 Service
- 当前已新增 auth 第一阶段 Service 和 SessionAuthGuard

## 3. 当前 Service

| Service 名称   | 所属模块 | 核心职责                                                       | 依赖 Model / Service / 外部服务 | 主要方法                                                                                      | 副作用                                | 不应承担的职责                                                              | 关联 API                      | 关联测试                                             | 备注                                                                                  |
| -------------- | -------- | -------------------------------------------------------------- | ------------------------------- | --------------------------------------------------------------------------------------------- | ------------------------------------- | --------------------------------------------------------------------------- | ----------------------------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------- |
| `AppService`   | app      | 提供最小健康检查响应                                           | 无                              | `getHealth()`                                                                                 | 无                                    | 不承载业务规则、认证、数据库访问或外部集成                                  | `GET /health`                 | `src/app.controller.spec.ts`、`test/app.e2e-spec.ts` | 仅用于证明后端骨架可运行                                                              |
| `UsersService` | users    | 提供用户基础创建、公开查询、认证内部身份查询和登录时间更新能力 | `UserModel`                     | `create()`、`createWithPlainPassword()`、`findById()`、`findByPhone()`、`findAuthIdentityByPhone()`、`updateLastLoginAt()` | 写入 `users` 集合；更新 `lastLoginAt` | 不负责登录、phone one-time code、Session、HTTP 契约或角色权限矩阵 | 无，本次未创建 Controller/API | `src/modules/users/users.service.spec.ts`、`test/project-imports.e2e-spec.ts` | `createWithPlainPassword()` 仅供受控服务端流程复用密码哈希；`findAuthIdentityByPhone()` 是唯一允许显式读取 `passwordHash` 的查询 |
| `SessionsService` | sessions | 提供服务端 session 创建、有效 token 查询、撤销、最近访问时间更新和旧 session 回收能力 | `SessionModel` | `createSession()`、`findValidByToken()`、`revokeByToken()`、`revokeAllForUser()`、`touchSession()`、`pruneOldSessionsForUser()` | 写入 `sessions` 集合；更新 `revokedAt`、`lastSeenAt` | 不负责 Cookie、登录、密码校验、phone one-time code、Guard、HTTP 契约或角色权限矩阵 | 无，本次未创建 Controller/API | `src/modules/sessions/sessions.service.spec.ts` | `createSession()` 是唯一返回 token 的方法；其他公开返回类型不包含 token |
| `AuthService` | auth | 编排手机号 + 密码登录、当前用户解析和登出 | `UsersService`、`SessionsService`、`ConfigService`、`bcryptjs.compare` | `login()`、`getCurrentUserFromToken()`、`logout()` | 创建 / 撤销 session；更新 `lastLoginAt`；触发旧 session 回收 | 不负责用户注册、找回密码、修改密码、phone one-time code、角色权限矩阵或 Cookie 写入 | `POST /auth/login`、`POST /auth/logout`、`GET /auth/me` | `src/modules/auth/auth.service.spec.ts`、`test/auth.e2e-spec.ts` | 对用户不存在、密码错误、用户禁用和 session 失效统一抛 `401`；token 不进入响应 body |
| `BatchesService` | batches | 批次 CRUD、分页查询、`name` 唯一检查 | `BatchModel` | `create()`、`list()`、`findById()`、`existsById()`、`update()`、`remove()` | 写入 `batches` 集合 | 不强制“一年只能一个批次” | `/admin/batches` | `test/admin-foundation.e2e-spec.ts` | `remove()` 为停用 |
| `DictionariesService` | dictionaries | 普通字典 CRUD、非分页过滤列表、同类型 code/name 唯一检查、按类型查找 | `DictionaryModel` | `create()`、`list()`、`findById()`、`findByIdAndType()`、`update()`、`remove()` | 写入 `dictionaries` 集合 | 不承载项目类型、学科、行政区划、受理处室等树形数据 | `/admin/dictionaries` | `test/admin-foundation.e2e-spec.ts` | `list()` 返回数组；项目状态通过 `dictType=project_status` 校验 |
| `TreeDictionariesService` | tree-dictionaries | 多 treeType 树节点 CRUD、非分页平铺过滤列表、父子层级/path 维护、同父同名约束 | `TreeDictionaryModel` | `create()`、`list()`、`findById()`、`findByIdAndType()`、`update()`、`remove()` | 写入 `tree_dictionaries` 集合 | 不内置国家行政区划或学科完整数据；不提供 tree children 接口 | `/admin/tree-dictionaries` | `test/admin-foundation.e2e-spec.ts` | `list()` 返回平铺数组；有子节点时 `remove()` 返回 `409` |
| `OrganizationsService` | organizations | 单位 CRUD、分页查询、`name` 唯一、行政区划节点校验、批量存在性校验 | `OrganizationModel`、`TreeDictionariesService` | `create()`、`list()`、`findById()`、`assertAllExist()`、`update()`、`remove()` | 写入 `organizations` 集合 | 不实现专家回避规则 | `/admin/organizations` | `test/admin-foundation.e2e-spec.ts` | `regionId` 字段名保持不变，必须引用 `treeType=administrative_division`；历史 `treeType=region` 不再作为行政区划；分页最大 `1000` |
| `ReviewSchemesService` | review-schemes | 评审方案 CRUD、非分页过滤列表、items 校验后总分计算、启用方案查找 | `ReviewSchemeModel` | `create()`、`list()`、`findById()`、`findActiveById()`、`update()`、`remove()` | 写入 `review_schemes` 集合 | 不实现项目使用后方案快照逻辑 | `/admin/review-schemes` | `test/admin-foundation.e2e-spec.ts` | `list()` 返回数组；`totalScore` 不信任前端，由后端计算 |
| `ProjectsService` | projects | 项目 CRUD、分页查询、项目编号唯一、跨模块关联校验；为导入确认提供受控创建/更新入口；评审负责人/方案设置；评审安排；评审负责人项目列表 | `ProjectModel`、`BatchesService`、`DictionariesService`、`TreeDictionariesService`、`OrganizationsService`、`ReviewSchemesService`、`UsersService` | `create()`、`list()`、`findById()`、`update()`、`remove()`、`upsertImportedProject()`、`updateReviewAssignment()`、`batchUpdateReviewAssignment()`、`updateSchedule()`、`listForReviewManager()`、`findActiveForReviewAssignment()`、`assertCanManageProject()` | 写入 `projects` 集合；设置 `reviewSchemeId` 时写入 `reviewSchemeSnapshot` | 不实现材料上传、专家评分、AI、申诉、看板或腾讯会议 API | `/admin/projects`、`/review-manager/projects`、`/admin/project-imports/*confirm*` | `test/admin-foundation.e2e-spec.ts`、`test/project-imports.e2e-spec.ts`、`test/project-review-assignments.e2e-spec.ts` | 分页最大 `1000`；负责人/评审负责人采用硬角色校验；`upsertImportedProject()` 不覆盖评审流程字段；评审安排只更新 `reviewTime/reviewLocation/meetingUrl` |
| `ProjectImportsService` | project-imports | Excel 项目导入上传、字段映射、行解析、自动匹配、待确认修正、确认入库、批量确认、跳过和统计刷新 | `ProjectImportJobModel`、`ProjectImportRowModel`、`BatchModel/Service`、`DictionaryModel`、`TreeDictionaryModel`、`OrganizationModel/Service`、`UserModel/Service`、`ProjectModel/Service`、`xlsx` | `upload()`、`listJobs()`、`findJobById()`、`listRows()`、`updateRow()`、`confirmRow()`、`confirmJob()`、`skipRow()` | 写入 `project_import_jobs`、`project_import_rows`，确认时写入或更新 `projects`，人工修正时可创建 `organizations` 和 `users` | 不保存原 Excel 文件；不接 OSS；不创建项目类型、学科、受理处室、项目状态等字典；不实现专家分配、材料、评分、AI、申诉或看板 | `/admin/project-imports` | `test/project-imports.e2e-spec.ts` | 字段映射为代码常量；匹配规则保守精确；导入任务/行列表分页最大 `1000` |
| `ExpertEligibilityService` | project-expert-assignments | 集中判断专家是否可分配给项目，输出 `allowed/reasons/detail` | `ProjectsService`、`UserModel` | `checkExpertForProject()` | 无直接写入 | 不执行分配写入、不做专家负载均衡或随机抽取 | 专家候选、单项目分配、批量专家分配内部复用 | `test/project-review-assignments.e2e-spec.ts` | 校验项目存在且启用、项目学科、专家存在且启用、`expert` 角色、专家学科、学科交集、承担单位回避、合作单位回避 |
| `ProjectExpertAssignmentsService` | project-expert-assignments | 专家候选分页、当前专家列表、追加专家、替换专家集合、移除专家、批量专家分配 | `ProjectExpertAssignmentModel`、`UserModel`、`ProjectsService`、`ExpertEligibilityService` | `listCandidates()`、`listAssignedExperts()`、`appendExperts()`、`replaceExperts()`、`removeExpert()`、`batchUpdateExperts()` | 写入 `project_expert_assignments`；移除为 `status=removed` | 不写入 Project 的专家冗余数组；不实现专家评分、专家负载均衡、随机抽取或通知 | `/review-manager/projects/:id/*experts*`、`/review-manager/projects/experts/batch`、`/admin/projects/:id/expert-candidates` | `test/project-review-assignments.e2e-spec.ts` | `projectId + expertUserId` 唯一；removed 后再次分配复用原记录并恢复 `assigned` |
| `StorageConfigService` | storage | 读取 Storage / OSS 环境配置并按驱动校验必需项 | `process.env` | `getDriver()`、`getObjectPrefix()`、`getFakeBucket()`、`getOssConfigOrThrow()` | 无 | 不记录真实 AccessKey、不执行上传或签名 URL | storage 内部复用 | `src/modules/storage/storage.service.spec.ts` | `STORAGE_DRIVER` 默认 production 为 `oss`、其他环境为 `fake`；`STORAGE_DRIVER=oss` 调用真实能力时校验 `OSS_*` |
| `FakeStorageService` | storage | 提供自动化测试和本地开发用 fake storage 行为 | `StorageConfigService` | `uploadFile()`、`getSignedUrl()`、`deleteObject()` | 无外部副作用 | 不访问真实 OSS、不长期保存文件内容 | 项目材料上传/下载 URL 内部复用 | `src/modules/storage/storage.service.spec.ts`、`test/project-materials.e2e-spec.ts` | 返回结构化 fake objectKey；签名 URL 形如 `https://fake-storage.local/{objectKey}?expires=...`；delete 为 no-op |
| `OssStorageService` | storage | 使用 `ali-oss` 上传、生成签名 URL 和最佳努力删除 OSS object | `StorageConfigService`、`ali-oss` | `uploadFile()`、`getSignedUrl()`、`deleteObject()` | 访问阿里云 OSS；上传/删除 object；生成短期签名 URL | 不保存真实密钥、不决定业务权限、不持久化材料元数据 | 项目材料上传/下载 URL 内部复用 | `src/modules/storage/storage.service.spec.ts` | 上传/删除使用 `OSS_INTERNAL_ENDPOINT`；签名 URL 使用 `OSS_PUBLIC_ENDPOINT`；配置缺失返回明确服务不可用错误 |
| `ProjectMaterialsService` | project-materials | 项目负责人项目视图、`followUpNeeds` 更新、材料上传/列表/下载 URL/软删除，以及评审负责人、专家、管理员只读材料可见性 | `ProjectMaterialModel`、`ProjectModel`、`DictionaryModel`、`ProjectExpertAssignmentModel`、`StorageService`、`StorageConfigService` | `listProjectOwnerProjects()`、`getProjectOwnerProject()`、`updateFollowUpNeeds()`、`listExpertProjects()`、`getExpertProject()`、`uploadProjectOwnerMaterials()`、`listMaterialsForProjectOwner()`、`listMaterialsForReviewManager()`、`listMaterialsForExpert()`、`listMaterialsForAdmin()`、`getDownloadUrlFor*()`、`softDeleteProjectOwnerMaterial()` | 写入 `projects.followUpNeeds`；写入 `project_materials`；调用 storage 上传、签名 URL 和上传失败清理 | 不实现专家评分、AI 合议、申诉、甲方看板、病毒扫描、OCR、在线预览转码、前端直传、分片上传或断点续传 | `/project-owner/*`、`/review-manager/projects/:id/materials*`、`/expert/*`、`/admin/projects/:id/materials*` | `test/project-materials.e2e-spec.ts` | 材料类型必须是启用 `dictType=material_type`；删除为软删除，不物理删除 OSS object；单项目材料列表不分页 |
| `ExpertReviewsService` | expert-reviews | 专家评分任务列表/详情、草稿保存、提交校验、评审负责人/管理员查看评分、退回评分、评分汇总，为合议提供 submitted 评分读取 | `ExpertReviewModel`、`ProjectModel`、`ProjectExpertAssignmentModel`、`ProjectMaterialModel`、`UserModel` | `listExpertTasks()`、`getExpertTask()`、`saveDraft()`、`submitReview()`、`listProjectExpertReviews()`、`getProjectExpertReview()`、`returnExpertReview()`、`getReviewSummary()`、`getSubmittedReviewsForConsensus()` | 写入 `expert_reviews`；退回时保留评分内容并写退回留痕 | 不修改 `ReviewScheme`；不重写已有评分快照；不实现申诉、导出、通知或复杂解锁审批 | `/expert/review-tasks*`、`/review-manager/projects/:id/expert-reviews*`、`/admin/projects/:id/expert-reviews*`、review-summary | `test/expert-reviews.e2e-spec.ts` | 专家评分必须使用 `Project.reviewSchemeSnapshot`；`totalScore` 后端计算；无评分视图状态为 `not_started` |
| `ConsensusReviewsService` | consensus-reviews | 规则化合议草稿生成、合议记录查看、人工确认合议、`finalLevel` 校验和项目等级写入 | `ConsensusReviewModel`、`ProjectModel`、`DictionaryModel`、`ExpertReviewsService` | `generateDraft()`、`getConsensus()`、`getConsensusForAdmin()`、`confirm()`、`confirmForAdmin()` | 写入 `consensus_reviews`；确认时写 `projects.finalLevel`，如 `originalLevel` 为空同步写入 | 不调用真实 AI/LLM；不实现申诉、等级变更完整历史、报告导出或电子签名 | `/review-manager/projects/:id/consensus*`、`/admin/projects/:id/consensus*` | `test/consensus-reviews.e2e-spec.ts` | 草稿 `draftSource=rule_based`；`draftSource=ai` 仅为后续预留，不代表已实现 AI |
| `ProjectAppealsService` | project-appeals | 项目负责人查看 confirmed 合议结果和等级历史、提交申诉、申诉附件上传/列表/下载 URL/软删除；评审负责人/管理员查看和处理申诉；申诉导致等级调整时写等级变更日志 | `ProjectAppealModel`、`ProjectAppealAttachmentModel`、`ProjectLevelChangeLogModel`、`ProjectModel`、`ConsensusReviewModel`、`DictionaryModel`、`StorageService`、`StorageConfigService` | `getOwnerConfirmedConsensus()`、`listOwnerLevelHistory()`、`listOwnerAppeals()`、`getOwnerAppeal()`、`createOwnerAppeal()`、`uploadOwnerAppealAttachments()`、`listOwnerAppealAttachments()`、`getOwnerAppealAttachmentDownloadUrl()`、`softDeleteOwnerAppealAttachment()`、`listReviewManagerAppeals()`、`getReviewManagerAppeal()`、`handleReviewManagerAppeal()`、`listAdminAppeals()`、`getAdminAppeal()`、`handleAdminAppeal()`、`listAdminLevelHistory()` | 写入 `project_appeals`、`project_appeal_attachments`、`project_level_change_logs`；调用 storage 上传和签名 URL；申诉有效且等级不同才写 `projects.finalLevel` | 不修改 `ConsensusReview.finalLevel`；不实现申诉撤回、多级复议、复杂等级变更审批、通知、报告导出、电子签名、材料预览转码或病毒扫描 | `/project-owner/projects/:id/consensus`、`/project-owner/projects/:id/appeals*`、`/review-manager/projects/:id/appeals*`、`/admin/projects/:id/appeals*`、`/admin/projects/:id/level-history` | `test/project-appeals.e2e-spec.ts` | 同一项目最多 3 次申诉；`submitted/processing` 未处理申诉互斥；申诉附件不使用 `material_type`；删除为软删除，不物理删除 OSS object；E2E 使用 fake storage |

## 4. 当前 Guard

| Guard 名称 | 所属模块 | 核心职责 | 依赖 | 使用范围 | 不应承担的职责 | 关联测试 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `SessionAuthGuard` | auth | 从 HttpOnly Cookie 对应名称读取 session token，校验 session 和用户，挂载 `request.user` | `AuthService`、`ConfigService` | 当前用于 `GET /auth/me` | 不负责 RolesGuard、角色权限矩阵、Cookie 写入或业务权限 | `test/auth.e2e-spec.ts` | 校验失败统一 `401` |
| `RolesGuard` | common | 读取 `@Roles(...)` 元数据并校验当前登录用户 roles | `Reflector` | 当前用于 `/admin/*` 控制器 | 不负责登录态校验、菜单权限、数据范围权限或复杂权限矩阵 | `test/admin-foundation.e2e-spec.ts` | 登录但缺少角色返回 `403` |

## 5. 维护规则

- 新增核心 Service 必须同步本文档
- 修改 Service 职责边界必须同步本文档
- 新增外部服务依赖必须同步本文档
- 局部私有方法调整不一定同步，除非影响职责理解
